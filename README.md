# 🩺 Мини-платформа «Пациент-Врач»

Простой учебный проект, демонстрирующий взаимодействие между **пациентом** и **врачом** через веб-интерфейс.  
Проект реализован без базы данных — все данные хранятся в JSON-файлах, а связь между пользователями обновляется **в реальном времени** через **SSE (Server-Sent Events)**.  

---

## 🚀 Возможности

- Авторизация для пациента и врача  
- Пациент может выбрать врача по ID  
- После выбора информация о враче отображается в кабинете пациента  
- Врач видит список пациентов, выбравших его  
- Список пациентов обновляется **мгновенно**, без перезагрузки страницы  
- Реализовано без фреймворков — только **Go (backend)** и **HTML, CSS, JS (frontend)**  

---

## 🧩 Технологии

| Компонент | Используемые технологии |
|------------|--------------------------|
| **Backend** | Go (net/http, encoding/json) |
| **Frontend** | Чистые HTML, CSS, JavaScript |
| **Данные** | Статические JSON-файлы |
| **Live-обновления** | Server-Sent Events (SSE) |

---

## 📂 Структура проекта

```

patient-doctor/
├─ main.go                 # Бэкенд на Go
├─ go.mod                  # Модуль Go
├─ data/
│  ├─ seed.json            # Исходные данные (пациенты и врачи)
│  └─ selections.json      # Создаётся автоматически при выборе врачей
└─ web/
├─ index.html           # Главная страница с выбором роли
├─ patient.html         # Кабинет пациента
├─ doctor.html          # Кабинет врача
└─ app.js               # Общие JS-функции для API

````

---

## ⚙️ Установка и запуск

1. Установи [Go](https://go.dev/dl/) версии **1.21+**  
2. Клонируй проект:
   ```bash
   git clone https://github.com/<твой-юзернейм>/patient-doctor.git
   cd patient-doctor
````

3. Запусти сервер:

   ```bash
   go run .
   ```
4. Открой браузер и перейди по адресу:

   ```
   http://localhost:8080/
   ```

---

## 🔑 Тестовые учётные данные

### 👤 Пациенты

| Логин   | Пароль | Имя           |
| ------- | ------ | ------------- |
| `peter` | `1234` | Пётр Иванов   |
| `aliya` | `1234` | Алия Серикова |

### 🩺 Врачи

| Логин    | Пароль | ФИО                     | Специальность | ID  |
| -------- | ------ | ----------------------- | ------------- | --- |
| `cardio` | `1234` | Сауле Тлеуова Ерлановна | Кардиолог     | 101 |
| `neuro`  | `1234` | Дамир Садыков Маратович | Невролог      | 102 |

---

## 🧠 Как это работает

1. Пациент авторизуется → выбирает врача из списка
2. Выбранный врач отображается в кабинете пациента
3. Врач видит всех пациентов, выбравших его
4. Если пациент изменяет выбор — список у врача обновляется **в реальном времени**

---

## 🛠️ Технические детали

* Сервер обслуживает файлы из папки `web/` с помощью `http.FileServer`
* API-эндпоинты начинаются с `/api/...`:

  * `POST /api/login` — вход по роли и логину
  * `GET /api/patient/me?id=` — данные пациента
  * `GET /api/doctors` — список врачей
  * `POST /api/patient/select-doctor` — выбор врача
  * `GET /api/doctor?id=` — кабинет врача
  * `GET /api/doctor/stream?id=` — SSE-поток для обновлений

---

## 🧾 Пример работы

1. Запусти сервер
2. Открой **два окна браузера**:

   * `http://localhost:8080/patient.html`
   * `http://localhost:8080/doctor.html`
3. Войди пациентом `peter/1234`, выбери врача `ID 101`
4. В кабинете врача `cardio/1234` сразу появится пациент Пётр Иванов

---

## 💾 Данные

Все данные хранятся в JSON-файлах:

* `data/seed.json` — начальные пользователи
* `data/selections.json` — создаётся автоматически для сохранения выборов пациентов
